#aplicacion en python que calcula el area de un triangulo

base = float(input('Introduce la base del triangulo: '))
altura = float(input('Introduce la altura del triangulo: '))

area = (base * altura) / 2

print(f'El area del triangulo es: {area:.2f}')